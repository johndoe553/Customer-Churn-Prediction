# Evaluation outputs

Selected aggregate metrics and figures are intentionally versioned for reproducibility and the deployed model-performance pages. They contain no credentials or raw customer records.

Regenerate them with:

```bash
python scripts/train_model.py
```
